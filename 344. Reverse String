class Solution {
    void recur(char[] s, int l, int r) {
        if(l >= r) return;
        
        char t = s[l];
        s[l] = s[r];
        s[r] = t;

        recur(s, l+1, r-1);
    }
    public void reverseString(char[] s) {
        int len = s.length;
        recur(s, 0, len-1);
    }
}
