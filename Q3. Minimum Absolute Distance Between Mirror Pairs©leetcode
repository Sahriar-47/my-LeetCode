import java.util.*;

class Solution {
    private int reverseInt(int x) {
        int rev = 0;
        while (x > 0) {
            int r = x % 10;
            rev = rev * 10 + r;
            x /= 10;
        }
        return rev;
    }

    public int minMirrorPairDistance(int[] nums) {

        Map<Integer, Integer> lastSeen = new HashMap<>();
        int ans = Integer.MAX_VALUE;

        int revVal = reverseInt(nums[0]);
        lastSeen.put(revVal, 0);
        for (int i = 1; i < nums.length; i++) {
            int val = nums[i];
            int rev = reverseInt(val);
            
            if (lastSeen.containsKey(val)) {
                int j = lastSeen.get(val);
                ans = Math.min(ans, Math.abs(i - j));
            }

            lastSeen.put(rev, i);
        }

        if(ans != Integer.MAX_VALUE)
            return ans;
        return -1;
    }
}
©leetcode
