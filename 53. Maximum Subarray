class Solution {
    public int maxSubArray(int[] nums) {
        int n = nums.length;

        int ans = Integer.MIN_VALUE, cur = 0;
        for(int i=0; i<n; i++) {
            cur += nums[i];
            ans = Math.max(ans, cur);
            cur = Math.max(cur, 0);
        }
        return ans;
    }
}
